use udp::packet::Packet;

mod cli;

fn main() -> ! {
    let p = Packet {
        ptyp: todo!(),
        nseq: todo!(),
        peer: todo!(),
        port: todo!(),
        data: todo!(),
    };

    // cli::run_and_exit()
}
